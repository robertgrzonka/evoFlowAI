{
  "name": "evoflowai-monorepo",
  "version": "1.0.0",
  "description": "evoFlowAI - Intelligent nutrition analysis application powered by AI",
  "private": true,
  "workspaces": [
    "backend",
    "web",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:web\"",
    "dev:gpt5": "concurrently \"npm run dev:backend:gpt5\" \"npm run dev:web\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:backend:gpt5": "cd backend && npm run dev:gpt5",
    "dev:web": "cd web && npm run dev",
    "build": "npm run build:shared && npm run build:backend && npm run build:web",
    "build:shared": "cd shared && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:web": "cd web && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../web && npm install && cd ../shared && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}